# ============================================================
# Legacy CRM — Docker Compose Environment
# cp .env.example .env && preencha os valores reais
# ============================================================

# --- MariaDB ---
DB_NAME=legacy
DB_USER=legacy_user
DB_PASSWORD=troque_esta_senha_2026
DB_ROOT_PASSWORD=troque_root_2026
DB_PORT=3307

# --- Backend ---
JWT_SECRET=gere_uma_string_aleatoria_de_64_chars_aqui
JWT_EXPIRES_IN=7d
FRONTEND_URL=http://SEU_IP_OU_DOMINIO

# --- Google Gemini AI (multimodal: texto + áudio + imagem) ---
GOOGLE_AI_API_KEY=AIzaSyDx7MYh64Nbr-r6a5h_K_KBrl9jASYSge0
GOOGLE_AI_MODEL=gemini-3.1-flash-lite-preview
GOOGLE_AI_MEDIA_MODEL=gemini-2.5-flash

# --- WhatsApp — Evolution API (Baileys, open source) ---
# Chave de autenticação da Evolution API (você define)
EVOLUTION_API_KEY=legacy-evolution-api-key-2026
# Nome da instância (aparece no QR Code)
WHATSAPP_INSTANCE=legacy-crm
WEBHOOK_SECRET=legacy-webhook-secret-2026
# WhatsApp Bridge connection (backend usa estas vars)
WHATSAPP_API_URL=http://localhost:8081
WHATSAPP_API_KEY=legacy-evolution-api-key-2026
